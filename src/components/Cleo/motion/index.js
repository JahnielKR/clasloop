// ─── Cleo motion — public API ───────────────────────────────────────────────
// The motion library for the Cleo mascot: idle limb gestures + the per-instance
// stylesheet. Pure (no React), so it can be unit-tested on its own.

export { MOOD_GESTURES } from "./gestures";
export { buildCleoCSS } from "./stylesheet";
